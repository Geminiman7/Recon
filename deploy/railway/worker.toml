[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "celery -A app.core.celery_app worker --loglevel=INFO --concurrency=2 --max-tasks-per-child=100"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
